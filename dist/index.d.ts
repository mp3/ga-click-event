export default class GAClickEvent {
    private trackingItems;
    constructor();
    updateEvents(): void;
    private setEvents();
    private sendClickEvent(event);
    private openLink(element);
}
